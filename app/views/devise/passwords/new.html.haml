%h2 忘记密码？
= semantic_form_for resource, as: resource_name, url: password_path(resource_name), html: { method: :post } do |f|
  = f.semantic_errors
  = f.inputs do
    = f.input :email, hint: "mal@firefly.com"
  = f.buttons do
    = f.commit_button "发送重置密码请求！"
= render :partial => "devise/shared/links"
